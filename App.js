import React, {Fragment} from 'react';
import {
  StyleSheet,
  View,
  Text,
} from 'react-native';

import Router from './src/Router';

const App = () => {
  return (
    <Router />
  );
};

const styles = StyleSheet.create({
  
});

export default App;
