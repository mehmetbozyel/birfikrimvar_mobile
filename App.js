import React, {Fragment} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const App = () => {
  return (
    <View style={{justifyContent:'center', alignItems:'center', flex: 1}}>
      <Text style={{fontSize:40}}>!!! BİR FİKRİM VAR !!!</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default App;
